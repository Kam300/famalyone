# Шаблоны визуализации семейного древа

## 🎨 Доступные шаблоны

### 1. Современный стиль (Modern)
Карточный дизайн с полной информацией о каждом члене семьи.

**Особенности:**
- Прямоугольные карточки с фото
- Фамилия, имя и отчество
- Размер фото: 80×80dp
- Размер карточки: 120×wrap_content
- Закругленные углы: 12dp

**Вид:**
```
┌──────────┐  ━━━  ┌──────────┐
│  [Фото]  │       │  [Фото]  │
│  Отец    │       │  Мать    │
│  Иванов  │       │ Иванова  │
└──────────┘       └──────────┘
      ▼
 [👦] [👧] [👦]
```

### 2. Классический стиль (Classic)
Традиционное древо с круглыми фото и минимальной информацией.

**Особенности:**
- Круглые фото (как на картинке)
- Только имя и фамилия
- Размер фото: 80×80dp (круг)
- Компактный вид
- Обводка: 2dp белая

**Вид:**
```
    ⭕  ━━━  ⭕
   Отец     Мать
  Иванов   Иванова
      ▼
  👦  👧  👦
```

## 🔄 Переключение шаблонов

### Через UI
1. Откройте "Древо семьи"
2. Нажмите кнопку переключения в правом верхнем углу (иконка 📋)
3. Выберите нужный шаблон
4. Древо автоматически перестроится

### Программно
```kotlin
// Сохранить выбор
TreeTemplateManager.saveTemplate(context, TreeTemplate.CLASSIC)

// Получить текущий
val template = TreeTemplateManager.getTemplate(context)
```

## 📊 Структура данных

### Enum TreeTemplate
```kotlin
enum class TreeTemplate {
    MODERN,     // Современный стиль
    CLASSIC     // Классический стиль
}
```

### Менеджер шаблонов
```kotlin
object TreeTemplateManager {
    fun saveTemplate(context: Context, template: TreeTemplate)
    fun getTemplate(context: Context): TreeTemplate
}
```

## 🎯 Применение шаблонов

### В FamilyTreeActivity

```kotlin
private var currentTemplate: TreeTemplate = TreeTemplate.MODERN

// Загрузка сохраненного шаблона
currentTemplate = TreeTemplateManager.getTemplate(this)

// Создание карточки с учетом шаблона
private fun createMemberCard(member: FamilyMember): View {
    return when (currentTemplate) {
        TreeTemplate.MODERN -> createModernMemberCard(member)
        TreeTemplate.CLASSIC -> createClassicMemberCard(member)
    }
}
```

## 📐 Layouts

### Modern Template
**Файл:** `item_tree_member.xml`

```xml
<MaterialCardView>
    <LinearLayout vertical>
        <ShapeableImageView 80×80 />
        <TextView name />
        <TextView details />
    </LinearLayout>
</MaterialCardView>
```

### Classic Template
**Файл:** `item_classic_tree_member.xml`

```xml
<LinearLayout vertical center>
    <MaterialCardView circular>
        <ShapeableImageView 80×80 />
    </MaterialCardView>
    <TextView name />
</LinearLayout>
```

## 🎨 Визуальные различия

| Параметр | Modern | Classic |
|----------|--------|---------|
| Форма фото | Квадрат (12dp радиус) | Круг (40dp радиус) |
| Информация | Фамилия + Имя + Отчество | Имя + Фамилия |
| Фон карточки | Фиолетовый | Прозрачный |
| Обводка | Нет | Белая 2dp |
| Размер | 120×auto | 100×auto |
| Высота карточки | ~140dp | ~110dp |

## 🔗 Линии связи

### Modern Template
- Ширина линии между парой: 40dp
- Прозрачность: 80%
- Цвет: белый

### Classic Template
- Ширина линии между парой: 30dp
- Прозрачность: 90%
- Цвет: белый
- Более четкие линии для традиционного вида

## 📱 Горизонтальное управление

### Настройки ScrollView

```xml
<HorizontalScrollView
    android:scrollbars="horizontal"
    android:scrollbarStyle="outsideInset"
    android:fadeScrollbars="false"
    android:scrollbarSize="8dp" />
```

**Особенности:**
- Постоянно видимый скроллбар
- Размер: 8dp для лучшей видимости
- Плавная прокрутка
- Автоматическое центрирование контента

### Вертикальное управление

```xml
<ScrollView
    android:scrollbars="vertical"
    android:scrollbarStyle="outsideInset"
    android:fadeScrollbars="false" />
```

## 💾 Сохранение настроек

### SharedPreferences
```kotlin
private const val PREFS_NAME = "tree_template_prefs"
private const val KEY_TEMPLATE = "selected_template"
```

**Данные сохраняются:**
- При выборе нового шаблона
- В локальном хранилище приложения
- Восстанавливаются при следующем запуске

## 🚀 Преимущества системы шаблонов

### Modern Template
1. ✅ Полная информация о члене семьи
2. ✅ Удобно для больших фамилий
3. ✅ Современный дизайн
4. ✅ Четкое разделение информации

### Classic Template
1. ✅ Компактный вид
2. ✅ Традиционное древо
3. ✅ Больше членов на экране
4. ✅ Акцент на фото
5. ✅ Как на классических постерах древа

## 🎯 Когда использовать

### Modern Template
- **Большие семьи** с длинными именами
- **Детальный просмотр** информации
- **Современный подход** к генеалогии
- **Печать** с полными данными

### Classic Template
- **Визуальное древо** для декора
- **Презентации** семейной истории
- **Большие поколения** (>10 человек)
- **Традиционный вид** для бабушек и дедушек

## 📝 Пример использования

### Современный стиль

```
Родители
┌──────────────┐  ━━━━  ┌──────────────┐
│   [Фото]     │        │   [Фото]     │
│   Иванов     │        │  Иванова     │
│   Иван       │        │  Мария       │
│  Иванович    │        │ Петровна     │
└──────────────┘        └──────────────┘
         ▼
  [👦80] [👧80] [👦80]
```

### Классический стиль

```
Родители
    ⭕          ⭕
   Иван      Мария
  Иванов    Иванова
        ▼
    👦 👧 👦
```

## 🔧 Технические детали

### Создание Modern карточки
```kotlin
private fun createModernMemberCard(member: FamilyMember): View {
    val binding = ItemTreeMemberBinding.inflate(layoutInflater)
    binding.tvTreeMemberName.text = member.lastName
    binding.tvTreeMemberDetails.text = "${member.firstName}\n${member.patronymic}"
    // ... загрузка фото
    return binding.root
}
```

### Создание Classic карточки
```kotlin
private fun createClassicMemberCard(member: FamilyMember): View {
    val binding = ItemClassicTreeMemberBinding.inflate(layoutInflater)
    binding.tvClassicMemberName.text = "${member.firstName}\n${member.lastName}"
    // ... загрузка круглого фото
    return binding.root
}
```

## 🎨 Кастомизация

### Изменение цветов
```xml
<!-- Modern -->
app:cardBackgroundColor="@color/purple_button_light"

<!-- Classic -->
app:strokeColor="@color/white"
```

### Изменение размеров
```xml
<!-- Modern -->
android:layout_width="120dp"

<!-- Classic -->
android:layout_width="100dp"
app:cardCornerRadius="40dp"
```

## 📱 UI элементы

### Кнопка переключения
```xml
<MaterialButton
    android:id="@+id/btnChangeTemplate"
    app:icon="@android:drawable/ic_menu_view"
    app:iconTint="?attr/colorOnPrimary" />
```

### Диалог выбора
```kotlin
AlertDialog.Builder(this)
    .setTitle(R.string.select_template)
    .setSingleChoiceItems(templates, currentSelection) { dialog, which ->
        // Переключение шаблона
    }
    .show()
```

## 🔄 Динамическое обновление

При смене шаблона:
1. Сохраняется выбор в SharedPreferences
2. Кешированные члены семьи используются повторно
3. Древо перестраивается с новым шаблоном
4. Показывается toast с подтверждением
5. Scroll позиция сбрасывается наверх

## 🌟 Будущие улучшения

- **Custom шаблон** - создание своего дизайна
- **Цветовые схемы** - разные палитры
- **Размер фото** - настройка размера
- **Экспорт** - сохранение в выбранном шаблоне
- **Печать** - оптимизация для печати

Теперь ваше древо можно отобразить в двух стилях! 🌳👨‍👩‍👧‍👦

