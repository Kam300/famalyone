# Обновление v1.3.0.2 - Связи членов семьи

## ✨ Новые функции

### 1. 🔗 Система связей между членами семьи

#### База данных
- Добавлены поля `fatherId` и `motherId` в модель `FamilyMember`
- Версия БД обновлена до 2 с автоматической миграцией
- Поле `patronymic` теперь необязательное (`String?`)

#### UI для выбора родителей
При создании/редактировании члена семьи:
- **Кнопка "Выбрать отца"** - выбор из списка мужчин (отцы, дедушки, дяди)
- **Кнопка "Выбрать мать"** - выбор из списка женщин (матери, бабушки, тёти)
- Диалог с возможностью очистить выбор
- Автоматическое отображение выбранных родителей при редактировании

#### Визуализация связей

**В списке членов семьи:**
- Новое поле "Родители: [Имена]" под датой рождения
- Фиолетовый курсив для лучшей читаемости
- Автоматически скрывается, если родители не указаны

**В семейном древе:**
- Красивые вертикальные линии между поколениями
- Стрелки ▼ указывают направление связей
- Визуальные индикаторы связи (белые линии с прозрачностью)

### 2. ✅ Отчество - необязательное поле
- Больше не требуется заполнять отчество
- Валидация убрана
- Поле может быть пустым

### 3. 🎨 Исправлен Status Bar

**Проблема:** Status bar (панель состояния Android) перекрывала кнопки приложения

**Решение:**
- Добавлен `android:fitsSystemWindows="true"` во все layouts
- Настроены темы с правильными флагами:
  - `android:windowDrawsSystemBarBackgrounds`
  - `android:fitsSystemWindows`
- Все экраны теперь корректно учитывают панель состояния

## 📝 Измененные файлы

### Модели данных
```kotlin
// FamilyMember.kt
data class FamilyMember(
    // ... existing fields ...
    val patronymic: String? = null,  // Теперь необязательное
    val fatherId: Long? = null,      // NEW
    val motherId: Long? = null       // NEW
)
```

### База данных
- `FamilyDatabase.kt` - версия 2, fallbackToDestructiveMigration

### UI компоненты

**AddMemberActivity:**
- Новые методы:
  - `loadAllMembers()` - загрузка всех членов
  - `setupParentSelectors()` - настройка селекторов родителей
  - `showParentSelector()` - диалог выбора родителя
- Кнопки выбора родителей в layout
- Загрузка выбранных родителей при редактировании

**MemberAdapter:**
- Новый параметр `allMembers: () -> List<FamilyMember>`
- Метод `buildParentsText()` - формирование текста родителей
- Отображение родителей в карточке

**FamilyTreeActivity:**
- Метод `addConnectionIndicator()` - визуальные линии связи
- Улучшенная структура с стрелками между поколениями

### Layouts

**activity_add_member.xml:**
```xml
<!-- Новые кнопки выбора родителей -->
<TextView text="Связи (необязательно):" />
<Button id="btnSelectFather" text="Выбрать отца" />
<Button id="btnSelectMother" text="Выбрать мать" />
```

**item_family_member.xml:**
```xml
<!-- Новое поле для отображения родителей -->
<TextView
    id="tvParents"
    textColor="@color/purple_button"
    textStyle="italic"
    visibility="gone" />
```

**Все activity layouts:**
- Добавлен `android:fitsSystemWindows="true"`

### Темы
- `themes.xml` и `themes-night.xml`:
  - `android:windowDrawsSystemBarBackgrounds="true"`
  - `android:fitsSystemWindows="true"`

## 🎯 Как использовать

### Создание члена семьи с родителями

1. Откройте "Добавить Члена Семьи"
2. Заполните основные поля (Имя, Фамилия обязательны)
3. Отчество можно оставить пустым
4. Прокрутите до раздела "Связи (необязательно)"
5. Нажмите "Выбрать отца" или "Выбрать мать"
6. Выберите из списка подходящих кандидатов
7. Сохраните

### Просмотр связей

**В списке:**
- Под датой рождения видно "Родители: [Имена]"

**В древе:**
- Поколения соединены визуальными линиями
- Стрелки показывают направление связей

## 🔧 Технические детали

### Миграция базы данных
```kotlin
.fallbackToDestructiveMigration()
```
**Важно:** Это пересоздаст БД, все данные будут потеряны при обновлении!

Для production используйте:
```kotlin
.addMigrations(MIGRATION_1_2)
```

### Пример миграции (для будущего):
```kotlin
val MIGRATION_1_2 = object : Migration(1, 2) {
    override fun migrate(database: SupportSQLiteDatabase) {
        database.execSQL(
            "ALTER TABLE family_members ADD COLUMN fatherId INTEGER"
        )
        database.execSQL(
            "ALTER TABLE family_members ADD COLUMN motherId INTEGER"
        )
    }
}
```

## 📊 Схема связей

```
Бабушка + Дедушка
       ↓
    Родители (Отец + Мать)
       ↓
      Дети
```

### Логика фильтрации родителей:

**Потенциальные отцы:**
- Пол: Мужской
- Роль: Отец, Дедушка, Дядя
- Исключен редактируемый член

**Потенциальные матери:**
- Пол: Женский
- Роль: Мать, Бабушка, Тётя
- Исключен редактируемый член

## ✅ Что исправлено

1. ✅ Status bar больше не перекрывает кнопки
2. ✅ Отчество необязательное
3. ✅ Добавлена система связей
4. ✅ Визуализация связей в списке
5. ✅ Линии связи в семейном древе
6. ✅ Возможность выбора родителей при создании

## 🚀 Запуск

```bash
# Очистка и пересборка (важно из-за изменений БД)
Build -> Clean Project
Build -> Rebuild Project

# Запуск
Run 'app'
```

**Примечание:** При первом запуске после обновления БД будет пересоздана!

## 📱 Скриншоты функций

### Экран добавления члена семьи
- Новая секция "Связи (необязательно)"
- Кнопки выбора отца и матери

### Список семьи
- Отображение родителей фиолетовым курсивом

### Семейное древо
- Визуальные линии между поколениями
- Стрелки направления связей

## 🎨 Дизайн связей

**Цвета:**
- Линии связи: белые, прозрачность 50%
- Текст родителей: фиолетовый (#5E43EC)
- Стрелки: белые, прозрачность 70%

**Размеры:**
- Толщина линий: 4dp
- Высота индикатора: 60dp
- Размер стрелки: 20sp

## 💡 Советы по использованию

1. **Сначала добавьте старшее поколение** (бабушки, дедушки)
2. **Затем родителей** и укажите связь с бабушками/дедушками
3. **Потом детей** с указанием родителей
4. В древе связи отобразятся автоматически

## 🐛 Известные ограничения

- Нельзя указать более 2 родителей (отец + мать)
- При выборе родителей не проверяется логическая корректность (возраст, пол)
- Линии связи в древе упрощенные (вертикальные + стрелки)

## 🔮 Будущие улучшения

- [ ] Графические линии между конкретными членами
- [ ] Проверка возраста родителей
- [ ] Множественные браки
- [ ] Приемные родители
- [ ] Экспорт с визуализацией связей

