# Система автоматических обновлений

## 🔄 Описание

Добавлена система проверки и установки обновлений через GitHub. Приложение автоматически проверяет наличие новых версий и предлагает их загрузить.

## Компоненты

### 1. UpdateChecker
Утилита для проверки и загрузки обновлений.

**Функции:**
- `checkForUpdates()` - проверка наличия обновлений
- `downloadUpdate()` - загрузка APK файла
- `installUpdate()` - установка загруженного обновления
- `compareVersions()` - сравнение версий

### 2. AboutActivity
Обновлен экран "О приложении" с кнопкой проверки обновлений.

**Функции:**
- Проверка обновлений по кнопке
- Отображение информации о версиях
- Загрузка и установка обновлений
- Обработка завершения загрузки

### 3. update.json
JSON файл на GitHub с информацией о последней версии.

**Структура:**
```json
{
  "version": "1.4.0",
  "versionCode": 14,
  "url": "https://dropbox.com/...",
  "releaseNotes": "Описание изменений",
  "minAndroidVersion": 24,
  "changelog": ["Изменение 1", "Изменение 2"]
}
```

## Как это работает

### 1. Проверка обновлений
```kotlin
val updateInfo = UpdateChecker.checkForUpdates(context)
if (updateInfo?.isUpdateAvailable == true) {
    // Показать диалог с предложением обновиться
}
```

### 2. Загрузка обновления
```kotlin
val downloadId = UpdateChecker.downloadUpdate(context, downloadUrl)
// DownloadManager загружает APK в фоне
```

### 3. Установка обновления
```kotlin
UpdateChecker.installUpdate(context, downloadId)
// Открывается установщик Android
```

## Настройка

### 1. Создайте репозиторий на GitHub
```
https://github.com/Kam300/URL
```

### 2. Добавьте файл update.json
Разместите файл `update.json` в корне репозитория или в ветке `main`.

### 3. URL для проверки обновлений
```kotlin
private const val UPDATE_JSON_URL = 
    "https://raw.githubusercontent.com/Kam300/URL/main/update.json"
```

### 4. Загрузите APK на Dropbox
1. Загрузите APK файл на Dropbox
2. Получите прямую ссылку (добавьте `?raw=1` в конец)
3. Укажите ссылку в `update.json`

## Разрешения

Добавлены в AndroidManifest.xml:
```xml
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
<uses-permission android:name="android.permission.DOWNLOAD_WITHOUT_NOTIFICATION" />
```

## Версионирование

### build.gradle.kts
```kotlin
versionCode = 14
versionName = "1.4.0"
```

### Формат версии
- **Major.Minor.Patch** (например, 1.4.0)
- **versionCode** - целое число, увеличивается с каждым релизом

### Сравнение версий
```
1.4.0 > 1.3.0
1.4.1 > 1.4.0
2.0.0 > 1.9.9
```

## Использование

### Для пользователей:
1. Откройте "О приложении"
2. Нажмите "Проверить обновления"
3. Если доступно обновление - нажмите "Загрузить"
4. После загрузки - нажмите "Установить"
5. Подтвердите установку в системном диалоге

### Для разработчиков:
1. Обновите версию в `build.gradle.kts`
2. Соберите APK
3. Загрузите APK на Dropbox
4. Обновите `update.json` на GitHub
5. Пользователи получат уведомление об обновлении

## Безопасность

### Проверки:
- ✅ HTTPS соединение
- ✅ Сравнение версий
- ✅ Проверка целостности загрузки
- ✅ Системный установщик Android

### Рекомендации:
- Используйте HTTPS для всех ссылок
- Проверяйте подпись APK
- Храните APK на надежном хостинге
- Регулярно обновляйте `update.json`

## Обработка ошибок

### Нет интернета:
```kotlin
try {
    val updateInfo = UpdateChecker.checkForUpdates(context)
} catch (e: Exception) {
    toast("Проверьте подключение к интернету")
}
```

### Ошибка загрузки:
```kotlin
try {
    downloadId = UpdateChecker.downloadUpdate(context, url)
} catch (e: Exception) {
    toast("Ошибка загрузки: ${e.message}")
}
```

### Ошибка установки:
- Проверьте разрешение `REQUEST_INSTALL_PACKAGES`
- Убедитесь, что APK не поврежден
- Проверьте совместимость версии Android

## Тестирование

### 1. Тест проверки обновлений:
1. Установите версию 1.3.0
2. Обновите `update.json` до 1.4.0
3. Нажмите "Проверить обновления"
4. ✅ Должно показать диалог с новой версией

### 2. Тест загрузки:
1. Нажмите "Загрузить"
2. ✅ Должна начаться загрузка
3. ✅ Уведомление в панели уведомлений
4. ✅ После завершения - диалог установки

### 3. Тест установки:
1. Нажмите "Установить"
2. ✅ Должен открыться установщик Android
3. ✅ После установки - новая версия

### 4. Тест "нет обновлений":
1. Установите последнюю версию
2. Нажмите "Проверить обновления"
3. ✅ Должно показать "У вас последняя версия"

## Примеры диалогов

### Доступно обновление:
```
Доступно обновление! 🎉

Текущая версия: 1.3.0
Новая версия: 1.4.0

Хотите загрузить обновление?

[Загрузить] [Позже] [GitHub]
```

### Нет обновлений:
```
Обновлений нет ✓

У вас установлена последняя версия!

Текущая версия: 1.4.0
Последняя версия: 1.4.0

[OK]
```

### Загрузка завершена:
```
Загрузка завершена

Обновление загружено. Установить сейчас?

[Установить] [Позже]
```

## Файлы

### Новые:
- ✅ `UpdateChecker.kt` - логика обновлений
- ✅ `update.json` - информация о версии

### Изменённые:
- ✅ `AboutActivity.kt` - UI для обновлений
- ✅ `AndroidManifest.xml` - разрешения
- ✅ `build.gradle.kts` - версия приложения
- ✅ `strings.xml` - строки для UI

## Roadmap

### Будущие улучшения:
- [ ] Автоматическая проверка при запуске
- [ ] Принудительные обновления
- [ ] Дельта-обновления (только изменения)
- [ ] Откат к предыдущей версии
- [ ] История обновлений
- [ ] Уведомления о новых версиях

---

**Версия:** 1.4.0
**Дата:** 17.11.2025
**Статус:** ✅ Реализовано
