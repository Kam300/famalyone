# ⚠️ Улучшенная обработка ошибок

## ✅ Реализовано

### Красивый диалог вместо toast
Теперь при ошибке регистрации лица показывается информативный диалог с кнопками действий.

## 📱 Типы ошибок

### 1. Несколько лиц на фото
**Сообщение:**
```
⚠️ Ошибка регистрации лица

На выбранном фото обнаружено несколько лиц.

Для регистрации в системе распознавания 
используйте фото с одним человеком.
```

**Кнопки:**
- **Выбрать другое фото** - открывает галерею заново
- **Пропустить** - сохраняет члена семьи без регистрации лица

### 2. Лицо не найдено
**Сообщение:**
```
⚠️ Ошибка регистрации лица

На выбранном фото не обнаружено лиц.

Попробуйте выбрать другое фото с четким 
изображением лица.
```

**Кнопки:**
- **Выбрать другое фото**
- **Пропустить**

### 3. Другие ошибки
**Сообщение:**
```
⚠️ Ошибка регистрации лица

Не удалось зарегистрировать лицо для распознавания.

[Текст ошибки от сервера]
```

**Кнопки:**
- **Выбрать другое фото**
- **Пропустить**

## 🎯 Преимущества

### Было (toast):
- ❌ Быстро исчезает
- ❌ Можно не заметить
- ❌ Нет возможности действий
- ❌ Непонятно что делать

### Стало (диалог):
- ✅ Блокирует экран - нельзя пропустить
- ✅ Четкое объяснение проблемы
- ✅ Кнопки с действиями
- ✅ Можно сразу выбрать другое фото
- ✅ Можно пропустить регистрацию

## 🔧 Как работает

### Определение типа ошибки
```kotlin
val message = when {
    errorMessage.contains("несколько лиц") -> 
        "На фото несколько лиц..."
    errorMessage.contains("не найдено лиц") ->
        "Лицо не найдено..."
    else ->
        "Общая ошибка..."
}
```

### Кнопка "Выбрать другое фото"
```kotlin
.setPositiveButton("Выбрать другое фото") { _, _ ->
    checkPermissionAndPickImage()
}
```

### Кнопка "Пропустить"
```kotlin
.setNegativeButton("Пропустить") { dialog, _ ->
    dialog.dismiss()
    toast("Член семьи сохранен без регистрации лица")
}
```

### Диалог нельзя закрыть случайно
```kotlin
.setCancelable(false)
```

## 📊 Сценарии использования

### Сценарий 1: Групповое фото
1. Пользователь выбирает фото с несколькими людьми
2. Сервер возвращает ошибку "несколько лиц"
3. Показывается диалог с объяснением
4. Пользователь нажимает "Выбрать другое фото"
5. Открывается галерея
6. Выбирает фото с одним человеком
7. Регистрация успешна!

### Сценарий 2: Фото без лица
1. Пользователь выбирает фото пейзажа
2. Сервер возвращает "лицо не найдено"
3. Показывается диалог
4. Пользователь выбирает правильное фото

### Сценарий 3: Пропуск регистрации
1. Ошибка регистрации
2. Пользователь нажимает "Пропустить"
3. Член семьи сохраняется
4. Можно зарегистрировать лицо позже

## ✨ Готово!

Теперь пользователи получают четкие инструкции при ошибках! 🎊
