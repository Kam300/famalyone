# Подключение к серверу - Решение проблемы

## ❌ Проблема: "Сервер недоступен"

Сервер запущен, но приложение не может к нему подключиться.

## ✅ Решение

### Вариант 1: Эмулятор Android

Если вы используете **эмулятор Android Studio**:

1. В приложении откройте "Галерея с AI"
2. Нажмите на иконку настроек (⚙️)
3. Введите: `http://10.0.2.2:5000`
4. Нажмите "Сохранить"

⚠️ **Важно:** `10.0.2.2` - это специальный адрес для эмулятора, который указывает на localhost компьютера.

### Вариант 2: Реальное устройство

Если вы используете **реальный телефон/планшет**:

#### Шаг 1: Узнайте IP компьютера

**Windows:**
```cmd
ipconfig
```
Найдите "IPv4 Address" в разделе вашей сети (обычно начинается с 192.168.x.x)

**Linux/macOS:**
```bash
ifconfig
# или
ip addr show
```

#### Шаг 2: Настройте firewall

**Windows:**
1. Откройте "Брандмауэр Windows"
2. Разрешите входящие подключения для Python
3. Или временно отключите firewall для тестирования

**Linux:**
```bash
sudo ufw allow 5000
```

#### Шаг 3: Убедитесь, что устройство и компьютер в одной сети

- Оба должны быть подключены к одному Wi-Fi роутеру
- Проверьте, что IP компьютера начинается с 192.168.x.x

#### Шаг 4: Введите IP в приложении

1. В приложении откройте "Галерея с AI"
2. Нажмите на иконку настроек (⚙️)
3. Введите: `http://192.168.1.XXX:5000` (ваш IP)
4. Нажмите "Сохранить"

## 🔍 Проверка подключения

### Проверка 1: Сервер запущен?

В консоли должно быть:
```
* Running on http://127.0.0.1:5000
* Running on http://172.19.0.1:5000
```

### Проверка 2: Порт открыт?

**Windows:**
```cmd
netstat -ano | findstr :5000
```

Должна быть строка с LISTENING

### Проверка 3: Доступ из браузера

Откройте в браузере:
- На компьютере: `http://127.0.0.1:5000/health`
- На телефоне: `http://192.168.1.XXX:5000/health` (ваш IP)

Должен вернуться JSON:
```json
{"status": "ok", "members_count": 0}
```

## 🐛 Частые ошибки

### Ошибка 1: "Connection refused"
**Причина:** Сервер не запущен или неправильный порт
**Решение:** Проверьте, что сервер запущен и работает на порту 5000

### Ошибка 2: "Connection timeout"
**Причина:** Firewall блокирует подключение
**Решение:** Разрешите порт 5000 в firewall

### Ошибка 3: "Network unreachable"
**Причина:** Устройство и компьютер в разных сетях
**Решение:** Подключите оба к одному Wi-Fi

### Ошибка 4: "127.0.0.1 не работает"
**Причина:** 127.0.0.1 - это localhost, работает только на том же устройстве
**Решение:** 
- Для эмулятора: используйте `10.0.2.2:5000`
- Для реального устройства: используйте IP компьютера

## 📝 Пример настройки

### Ваша ситуация:
- Сервер запущен на: `http://127.0.0.1:5000` и `http://172.19.0.1:5000`
- IP компьютера: `172.19.0.1` (это Docker/WSL адрес)

### Что делать:

#### Для эмулятора:
```
URL: http://10.0.2.2:5000
```

#### Для реального устройства:
1. Узнайте реальный IP компьютера в локальной сети (не 172.19.0.1)
2. Используйте: `http://[ваш_IP]:5000`

## 🎯 Быстрое решение

### Вариант A: Используйте эмулятор
1. Запустите эмулятор Android Studio
2. В приложении установите URL: `http://10.0.2.2:5000`
3. Готово!

### Вариант B: Используйте ngrok (для реального устройства)
```bash
# Установите ngrok
# Запустите туннель
ngrok http 5000
```

Получите публичный URL (например: `https://abc123.ngrok.io`)
Используйте его в приложении.

## 💡 Рекомендации

1. **Для разработки:** Используйте эмулятор с `10.0.2.2:5000`
2. **Для тестирования на устройстве:** Используйте IP компьютера
3. **Для production:** Разверните сервер на облачном хостинге

---

**Нужна помощь?** Telegram: @toatalCode
